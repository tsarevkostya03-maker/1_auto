plugins {
    id 'java'
}

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.testng:testng:7.1.0'   // ← ЭТА СТРОЧКА ДОЛЖНА БЫТЬ!
}

test {
    useTestNG()    // ← И ЭТА СТРОЧКА ТОЖЕ!
}